# Verification: foundations

How to run this file: most items are observable through a scratch-folder scan (`/tmp/dedupe-verify`-style fixture) and the web UI over it. Keep the hash cache between items unless the item says to clear it (`rm ~/.cache/dedupe/hashes.sqlite3`). Device column values: `cli` = terminal run of `.venv/bin/dedupe`; `ui` = browser at `http://127.0.0.1:8765`; `disk` = inspect files on disk.

## foundations/scan-pipeline.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SCANP-01 | P1 | cli | Only image/GIF/video extensions are examined; other files are ignored ([What a scan examines](../foundations/scan-pipeline.md#what-a-scan-examines)). | Scratch folder with 2 JPEGs, 1 `.txt`, 1 `.pdf`. | 1. Run `.venv/bin/dedupe scan FOLDER`.<br>2. Read "Files scanned" in the summary. | Files scanned = 2; no errors mentioning the txt/pdf. | pass (scripted pass, 2026-09-03; also `tests/test_scanner.py`) |
| SCANP-02 | P1 | cli | Byte-identical files form one exact group via the size → partial hash → SHA-256 funnel ([How each detection works](../foundations/scan-pipeline.md#how-each-detection-works)). | Two byte-identical JPEGs plus a same-size different-content JPEG. | 1. Scan.<br>2. Read "Exact groups". | Exact groups = 1 (the identical pair); the same-size different file is not grouped. | pass (scripted pass: identical pair grouped; see scan summary, 2026-08-24) |
| SCANP-03 | P1 | cli | A resized copy forms a similar group (thresholds: global pHash ≤ 6, tiles ≤ 8/mean ≤ 5) ([How each detection works](../foundations/scan-pipeline.md#how-each-detection-works)). | One 1600×1200 JPEG and its ~97% resize. | 1. Scan.<br>2. Read "Similar groups". | Similar groups = 1 containing both files. | pass (scripted pass: "Found 1 similar image group", summary Similar groups: 1) |
| SCANP-04 | P1 | cli | A second scan reuses the hash cache ([The stages, in order](../foundations/scan-pipeline.md#the-stages-in-order)). | A folder already scanned once, unchanged. | 1. Run the scan again.<br>2. Read the cache line and the Diagnostics line. | "Cache hits: N/N" where N is the file count; duration drops sharply. | pass (scripted pass: second scan "Cache hits: 4/4", 0.49s → 0.04s) |
| SCANP-05 | P1 | cli | Photos.app library roots are refused with an export message ([What a scan examines](../foundations/scan-pipeline.md#what-a-scan-examines)). | A macOS machine with a Photos library. | 1. `dedupe scan ~/Pictures/Photos\ Library.photoslibrary`.<br>2. Read the errors. | Scan refuses the root with "Photos libraries cannot be scanned directly; export media from Photos.app…". | pass (scripted pass, 2026-09-03, against a fake `Fake Library.photoslibrary` directory — the check is name-based, no real library needed) |
| SCANP-06 | P2 | cli | Corrupt files never abort the scan; failures land in diagnostics ([Cancellation and failure](../foundations/scan-pipeline.md#cancellation-and-failure)). | A truncated JPEG (first 1 KB of a real one) beside good files. | 1. Scan.<br>2. Read the Diagnostics block. | Scan completes exit 0; a stage reports the failure count/warning. | pass (scripted pass, 2026-09-03: "Diagnostics: 2 failed across scan stages" with a truncated JPEG) |
| SCANP-07 | P2 | ui | Groups stream into the sidebar during the scan, most-reclaimable-first ([Streaming and progress](../foundations/scan-pipeline.md#streaming-and-progress)). | A folder large enough that the scan takes several seconds. | 1. Start a scan in the UI.<br>2. Watch the sidebar while the progress panel is active. | Group cards appear incrementally before "done"; order is by reclaimable size. | pass (e2e, 2026-09-03: `test_scan_streams_groups_and_cancel_restores_previous` observes group items while the scan runs) |
| SCANP-08 | P2 | ui | Multiple folders default to parallel streams with per-folder progress and no cross-folder groups ([One pool vs parallel streams](../foundations/scan-pipeline.md#one-pool-vs-parallel-streams)). | Two folders, each holding one copy of the same image. | 1. Scan both folders in the UI with default options.<br>2. Inspect the streams panel and the results.<br>3. Repeat with one-pool forced if the toggle is visible. | Streams mode: one progress line per folder; no group contains files from both folders. One pool: the cross-folder exact group appears. | pass (e2e, 2026-09-03: `test_parallel_streams_toggle_controls_cross_folder_groups`; CLI half also pass scripted, pooled=1 vs parallel=0) |
| SCANP-09 | P2 | cli | Cancellation is cooperative and leaves nothing behind but cache writes ([Cancellation and failure](../foundations/scan-pipeline.md#cancellation-and-failure)). | A large folder. | 1. Start `.venv/bin/dedupe scan FOLDER` in a TTY.<br>2. Press Ctrl+C during hashing. | The process exits; no result JSON, no review session; a rerun hits the cache for files hashed so far. | pass (scripted SIGINT, 2026-09-03: exit 130 with "scan cancelled"; rerun reported "Cache hits: 900/900" — cancel-time cache save added this run, before it the rerun showed 0) |

Not checkable by hand:

- Worker-count enforcement at the stage caps (4/6/4/4) — needs instrumentation, not observation.

## foundations/duplicate-group.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| GROUP-01 | P1 | ui | New exact/similar groups arrive pre-selected: every member except the suggested keeper ([The initial selection](../foundations/duplicate-group.md#the-initial-selection)). | The SCANP-02 fixture scanned in the UI. | 1. Open the exact group's detail pane.<br>2. Count checked checkboxes and the green card. | All members checked except one; the unchecked one is green (the keeper). | pass (e2e, 2026-08-24: `test_local_review_workflow` asserts 1 selected / 1 keeper / 1 checked) |
| GROUP-02 | P1 | ui | A keep-one group cannot have every member selected; the keeper is restored ([What actually acts](../foundations/duplicate-group.md#what-actually-acts-the-effective-selection)). | An exact group with 2 members in the UI. | 1. Check the keeper's checkbox too (via the card).<br>2. Reload the group payload or reopen the group. | Exactly one member ends up unselected; the selection never covers all members. | pass (automated + e2e, 2026-09-03: server rule in `test_bulk_selection_keeps_one_member_of_every_duplicate_group`; Space/u flow in `test_attention_navigation_and_space_u_shortcuts`) |
| GROUP-03 | P1 | ui | A similar group wholly contained in an exact group is dropped ([How groups are formed](../foundations/duplicate-group.md#how-groups-are-formed)). | Two identical files plus a resized copy (the SCANP-02/03 fixture). | 1. Scan.<br>2. Inspect the similar group's membership. | One similar group with all three members (mixed set survives); no separate similar group of just the identical pair. | pass (scripted pass: similar group n3 = orig + copy + resized in the isolate layout) |
| GROUP-04 | P2 | ui | Selection rules keep exactly one member: newest/oldest/largest/smallest/shortest path ([Selection rules](../foundations/duplicate-group.md#selection-rules)). | An exact group of 3+ members differing in size and mtime. | 1. Apply each rule via the rule chooser (`s`).<br>2. Check which member is kept. | Each rule keeps the member matching its criterion; all others selected. | pass (automated, 2026-09-03: rule semantics in `tests/test_grouping.py`; the `s` chooser dialog itself pending a hand look) |
| GROUP-05 | P2 | ui | Reclaimable bytes shown equal total minus keeper for duplicate groups ([What actually acts](../foundations/duplicate-group.md#what-actually-acts-the-effective-selection)). | Any exact group. | 1. Note member sizes.<br>2. Read the group's reclaimable figure. | Figure = sum(members) − size(keeper). | pass (automated, 2026-09-03: reclaimable-byte assertions in `tests/test_grouping.py`) |

## foundations/review-session.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SESSION-01 | P1 | disk | The session file is written on scan completion with private permissions ([Where the session lives](../foundations/review-session.md#where-the-session-lives)). | A completed scan. | 1. `ls -l ~/.local/state/dedupe/review-session.json`.<br>2. `stat` the directory. | File mode 600, directory 700; file mtime ≈ scan completion. | pass (scripted pass, 2026-09-03: file 0o600, dir 0o700 after `--load` startup with a scratch HOME) |
| SESSION-02 | P1 | ui | Files changed since the save are pruned on resume with reasons and up to 20 examples ([What a resume does](../foundations/review-session.md#what-a-resume-does-event-by-event)). | A scanned scratch folder; restart the server after modifying one file. | 1. Scan, then touch/resize one scanned file.<br>2. Restart `.venv/bin/dedupe ui`.<br>3. Read the resumed banner. | Banner reports the pruned count with reason "changed since the scan" and lists the file under "What was dropped?". | pass (e2e, 2026-09-03: `test_resume_banner_reports_pruned_files_and_discard_starts_clean`) |
| SESSION-03 | P1 | ui | Resume restores selections, not just groups ([Resuming](../foundations/review-session.md#resuming)). | A scan with a manual deselection made; restart the server. | 1. Deselect one member in a group.<br>2. Restart the server, reopen the page.<br>3. Open the same group. | The deselection persists; the group's selection matches the pre-restart state minus pruned files. | pass (e2e, 2026-09-03: `test_selections_survive_a_server_restart`) |
| SESSION-04 | P2 | disk | A corrupt session file is reported, not loaded ([Loading](../foundations/review-session.md#loading)). | Write `not json` into the session file, then start the UI. | 1. Overwrite the session file with invalid JSON.<br>2. Start the UI and inspect startup state. | App starts clean (no resumed results); the file is left in place. | pass (automated, 2026-09-03: `test_corrupt_session_metadata_flags_the_unreadable_file`, `test_corrupt_and_future_review_sessions_are_preserved`) |
| SESSION-05 | P2 | ui | A keep-one group pruned to one member disappears entirely ([Pruning](../foundations/review-session.md#pruning)). | An exact 2-member group; delete one member's file on disk; restart. | 1. Remove one of the two files.<br>2. Restart and inspect the group list. | The group is absent; the surviving file appears in no duplicate group. | pass (automated, 2026-09-03: `test_review_session_prunes_changed_files_and_dissolves_group`) |

## foundations/actions-and-undo.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ACTIONS-01 | P1 | ui | Trash executes per-file revalidation and writes a receipt ([The safety model](../foundations/actions-and-undo.md#the-safety-model)). | Scratch folder scanned; a selection in an exact group. | 1. Preview Trash in the action sheet.<br>2. Confirm.<br>3. Inspect `~/.cache/dedupe/logs/` and the macOS Trash. | Files in Trash; a new `action-…json` receipt lists each file and destination. | pass (e2e + scripted, 2026-09-03: `test_executed_trash_can_be_undone_from_the_result_toast`; scripted CLI trash execute wrote the receipt used to restore) |
| ACTIONS-02 | P1 | ui | A file changed between preview and confirm is excluded at execute ([Immediate revalidation](../foundations/actions-and-undo.md#the-safety-model)). | A selected file; the sheet open with a valid token. | 1. Preview Trash.<br>2. Append a byte to one selected file on disk.<br>3. Confirm. | The changed file is reported failed ("changed since scan"-style reason); the others move. | pass (automated, 2026-09-03: `test_execute_refuses_when_selected_file_changed_after_scan`) |
| ACTIONS-03 | P1 | ui | Review-category selections quarantine into `_Dedupe Quarantine` beside the scan root instead of system Trash ([Actions and undo in the UI](../ui/action-sheet.md#complete)). | Selections in both an exact group and the Low-res category. | 1. Confirm **Delete All Selected Exact Matches**.<br>2. Open **Delete All Selected Low-res + Random**, read the sheet's note, confirm.<br>3. Inspect the Trash and the scan root. | Exact-group file in system Trash; low-res file in `{root}/_Dedupe Quarantine`; the review sheet led with the split and its Confirm read "Move to Quarantine". | pass (automated + e2e, 2026-09-03: routing in `test_trash_routes_review_decisions_to_dedicated_quarantine`; sheet copy in the low-res browser test) |
| ACTIONS-04 | P1 | cli | Quarantine undo is all-or-nothing on preflight ([Undo](../foundations/actions-and-undo.md#undo)). | An executed quarantine receipt; one original path re-created with a new file. | 1. `dedupe undo RECEIPT` (preview).<br>2. Read the per-item results. | The occupied item reports "original path is already occupied" and every other item reports "undo cancelled because another file failed preflight"; nothing moves. | pass (scripted pass, 2026-09-03, UNDO-04 run) |
| ACTIONS-05 | P2 | ui | A symbolic link replacing a scanned file is refused by action preflight ([The safety model](../foundations/actions-and-undo.md#the-safety-model)). | Replace one selected file with a symlink to elsewhere. | 1. Preview Trash. | The symlinked path is reported refused ("symbolic link" refusal), not moved. | pass (automated, 2026-09-03: `test_trash_preview_refuses_a_symlinked_member`) |
| ACTIONS-06 | P2 | disk | Receipts land in `~/.cache/dedupe/logs/` with executed/preview prefixes ([The receipt](../foundations/actions-and-undo.md#the-safety-model)). | Any executed action and any preview. | 1. `ls ~/.cache/dedupe/logs/`. | Files named `action-{stamp}…json` and `preview-{stamp}…json`. | pass (scripted pass: both prefixes observed in the receipts directory) |
